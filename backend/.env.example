# Google Cloud Vision API credentials
# Option 1: Path to your service account JSON key file (for local development)
GOOGLE_APPLICATION_CREDENTIALS=path/to/your-service-account-key.json

# Option 2: Full JSON content of the service account key (for cloud deployment like Render)
# Copy the entire content of your service account JSON file here
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Server port (default: 5000)
PORT=5000
